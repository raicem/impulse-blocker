const settings = {
  SHOW_ON_OFF_BUTTONS_IN_POPUP: 'showOnOffButtonsInPopup',
  SHOW_PAUSE_BUTTONS_IN_POPUP: 'showPauseButtonsInPopup',
  HOLD_TO_CONFIRM_PAUSE: 'holdToConfirmPause',
  ON: 'on',
  OFF: 'off',
};

Object.freeze(settings);

export default settings;
